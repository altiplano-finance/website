---
import { url } from '../utils';

export interface Props {
  path?: string;
}
const { path = '/' } = Astro.props;

const links = [
  { href: '/', label: 'Home' },
  { href: '/services', label: 'Services' },
  { href: '/portfolio', label: 'Our Work' },
  { href: '/insights', label: 'Insights' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' },
];

const isActive = (href: string) =>
  href === '/' ? path === '/' : path.startsWith(href);
---

<header class="site-header" data-header>
  <div class="container header-inner">
    <a class="brand" href={url('/')} aria-label="Altiplano Strategic Finance — home">
      <img
        class="brand-mark"
        src={url('/logos/altiplano_navy_llama_cutout.png')}
        alt=""
        width="44"
        height="44"
        decoding="sync"
        fetchpriority="high"
      />
      <span class="brand-name">Altiplano</span>
    </a>

    <nav class="nav" aria-label="Primary">
      <ul class="nav-list">
        {
          links.map((l) => (
            <li>
              <a
                href={url(l.href)}
                class:list={['nav-link', { active: isActive(l.href) }]}
                aria-current={isActive(l.href) ? 'page' : undefined}
              >
                {l.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <button
      class="nav-toggle"
      type="button"
      aria-label="Open menu"
      aria-expanded="false"
      aria-controls="mobile-nav"
      data-nav-toggle
    >
      <span class="nav-toggle-bar"></span>
      <span class="nav-toggle-bar"></span>
      <span class="nav-toggle-bar"></span>
    </button>
  </div>

  <!-- Mobile drawer -->
  <div class="mobile-nav" id="mobile-nav" data-mobile-nav hidden>
    <ul class="mobile-nav-list">
      {
        links.map((l) => (
          <li>
            <a
              href={url(l.href)}
              class:list={['mobile-nav-link', { active: isActive(l.href) }]}
              aria-current={isActive(l.href) ? 'page' : undefined}
            >
              {l.label}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(245, 237, 230, 0.85);
    backdrop-filter: saturate(140%) blur(10px);
    -webkit-backdrop-filter: saturate(140%) blur(10px);
    border-bottom: 1px solid transparent;
    transition: border-color 0.3s var(--ease), background 0.3s var(--ease);
  }

  .site-header[data-scrolled] {
    border-bottom-color: var(--line);
    background: rgba(245, 237, 230, 0.94);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    min-height: 54px;
  }

  .brand {
    display: inline-flex;
    flex-direction: row;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
    line-height: 1;
  }

  .brand-mark {
    height: 30px;
    width: auto;
    flex-shrink: 0;
  }

  /* "ALTIPLANO" — Cinzel, navy, sized to stand at the height of the icon.
     The trailing letter-spacing adds a gap after the last letter; the
     matching negative margin pulls the box back so it stays aligned. */
  .brand-name {
    font-family: var(--font-logo);
    font-weight: 600;
    /* Fixed size (not a vw-based clamp) so the logo is identical on every page.
       Sized down to sit close to the nav link text. */
    font-size: calc(1.3rem * var(--text-scale));
    line-height: 1;
    color: var(--navy);
    text-transform: uppercase;
    letter-spacing: 0.32em;
    margin-right: -0.32em;
  }

  .nav-list {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 2.4vw, 2.25rem);
    list-style: none;
  }

  .nav-link {
    font-size: calc(0.94rem * var(--text-scale));
    font-weight: 500;
    color: var(--navy);
    letter-spacing: 0.005em;
    padding: 0.25rem 0;
    position: relative;
  }

  .nav-link::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -2px;
    height: 1.5px;
    width: 0;
    background: var(--clay);
    transition: width 0.25s var(--ease);
  }

  .nav-link:hover,
  .nav-link.active {
    color: var(--clay);
  }

  .nav-link.active::after,
  .nav-link:hover::after {
    width: 100%;
  }

  /* --- Hamburger --- */
  .nav-toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 44px;
    height: 44px;
    padding: 10px;
    background: transparent;
    border: 0;
    cursor: pointer;
  }

  .nav-toggle-bar {
    display: block;
    height: 1.5px;
    width: 100%;
    background: var(--navy);
    transition: transform 0.3s var(--ease), opacity 0.2s var(--ease);
  }

  .nav-toggle[aria-expanded='true'] .nav-toggle-bar:nth-child(1) {
    transform: translateY(6.5px) rotate(45deg);
  }
  .nav-toggle[aria-expanded='true'] .nav-toggle-bar:nth-child(2) {
    opacity: 0;
  }
  .nav-toggle[aria-expanded='true'] .nav-toggle-bar:nth-child(3) {
    transform: translateY(-6.5px) rotate(-45deg);
  }

  /* --- Mobile drawer ---
     A narrow column pinned to the right edge below the sticky header. It
     overlays the page instead of pushing content down, and only covers the
     width its links need. */
  .mobile-nav {
    display: none;
    position: absolute;
    top: 100%;
    right: 0;
    width: 220px;
    max-width: calc(100vw - 3rem);
    max-height: calc(100vh - 100%);
    overflow-y: auto;
    background: var(--sand);
    border: 1px solid var(--line);
    border-right: 0;
    border-radius: 0 0 0 var(--radius);
    box-shadow: -12px 18px 40px -20px rgba(0, 20, 33, 0.35);
  }

  .mobile-nav-list {
    list-style: none;
    padding: 0.25rem 1.5rem 0.75rem;
  }

  .mobile-nav-list li:last-child .mobile-nav-link {
    border-bottom: 0;
  }

  .mobile-nav-link {
    display: block;
    padding: 0.9rem 0;
    font-size: calc(1.05rem * var(--text-scale));
    font-weight: 500;
    color: var(--navy);
    border-bottom: 1px solid var(--line);
  }

  .mobile-nav-link.active {
    color: var(--clay);
  }

  @media (max-width: 860px) {
    .nav {
      display: none;
    }
    .nav-toggle {
      display: flex;
    }
    .mobile-nav:not([hidden]) {
      display: block;
    }
  }

  /* Scale the wordmark down on small phones so it never overflows.
     Still uniform across pages — this keys off width, not page. */
  @media (max-width: 420px) {
    .brand-mark {
      height: 26px;
    }
    .brand-name {
      font-size: calc(1.15rem * var(--text-scale));
    }
  }
</style>

<script>
  const header = document.querySelector('[data-header]') as HTMLElement | null;
  const toggle = document.querySelector('[data-nav-toggle]') as HTMLButtonElement | null;
  const drawer = document.querySelector('[data-mobile-nav]') as HTMLElement | null;

  if (toggle && drawer) {
    const setOpen = (open: boolean) => {
      toggle.setAttribute('aria-expanded', String(open));
      toggle.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
      drawer.hidden = !open;
    };
    toggle.addEventListener('click', () => {
      setOpen(toggle.getAttribute('aria-expanded') !== 'true');
    });
    drawer.addEventListener('click', (e) => {
      if ((e.target as HTMLElement).closest('a')) setOpen(false);
    });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') setOpen(false);
    });
    // The drawer overlays the page, so tapping the page behind it closes it.
    document.addEventListener('click', (e) => {
      if (!drawer.hidden && !(e.target as HTMLElement).closest('[data-header]')) {
        setOpen(false);
      }
    });
  }

  if (header) {
    const onScroll = () => {
      if (window.scrollY > 8) header.setAttribute('data-scrolled', '');
      else header.removeAttribute('data-scrolled');
    };
    onScroll();
    window.addEventListener('scroll', onScroll, { passive: true });
  }
</script>
